name: paper_chat
description: "Paper & Ink — Research paper reader with AI chat"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.2.1
  shared_preferences: ^2.5.0
  flutter_markdown_plus: ^1.0.5
  path_provider: ^2.1.5
  file_picker: ^13.1.0
  http: ^1.2.0
  flutter_math_fork: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
