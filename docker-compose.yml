version: '3.8'

services:
  grobid:
    image: lfoppiano/grobid:0.8.0
    container_name: grobid
    ports:
      - "8070:8070"
    environment:
      - GROBID_MAX_THREADS=4
    restart: unless-stopped
